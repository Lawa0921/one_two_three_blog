<nav class="main-navbar navbar navbar-light">
  <%= link_to t('name'), root_path, class:"nav-logo standard-link"%>
  <div>
    <ul class="navbar-nav ml-auto">
      <% if user_signed_in? %>
        <li class="nav-item dropdown" style="position: relative">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= t('setting')%>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown" style="position: absolute;">
            <%= link_to t("user.setting"), edit_user_registration_path, class:"dropdown-item"%>
            <%= link_to t("sign_out"), destroy_user_session_path, method: :delete, class:"dropdown-item" %>
          </div>
        </li>
        <li class="nav-item mx-3" style="min-width: 30px;">
          <% if current_user.avatar.attached? %>
            <%= image_tag current_user.avatar.variant(resize_to_limit: [30, 30]), class:"nav-link"%>
          <% end %>
        </li>
      <% else %>
        <li class="nav-item mx-3">
          <%= link_to t("sign_up"), new_user_registration_path, class:"nav-link" %>
        </li>
        <li class="nav-item mx-3">
          <%= link_to t("sign_in"), new_user_session_path, class:"nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
